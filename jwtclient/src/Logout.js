import React from 'react'

function Logout() {
    const handleLogout = ()=>{
        
    }
  return (
    <div>
        <button onClick={handleLogout} >
            logout
        </button>
    </div>
  )
}

export default Logout